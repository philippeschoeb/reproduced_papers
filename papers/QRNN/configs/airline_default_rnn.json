{
  "description": "Default vanilla RNN baseline on the airline-passengers dataset (shared data/time_series)",
  "dataset": {
    "name": "airline_passengers",
    "path": "../time_series/airline-passengers.csv",
    "kaggle_dataset": null,
    "preprocess": null,
    "time_column": "Month",
    "target_column": "Passengers",
    "feature_columns": ["Passengers"],
    "sequence_length": 12,
    "prediction_horizon": 1,
    "train_ratio": 0.7,
    "val_ratio": 0.15,
    "batch_size": 16,
    "shuffle": true
  },
  "model": {
    "params": {
      "cell_type": "rnn",
      "hidden_dim": 32,
      "layers": 2,
      "dropout": 0.0,
      "input_dropout": 0.0,
      "bidirectional": false
    }
  },
  "training": {
    "epochs": 80,
    "optimizer": "adam",
    "lr": 0.001,
    "weight_decay": 0.0,
    "clip_grad_norm": 1.0,
    "early_stopping": {
      "enabled": true,
      "patience": 20,
      "min_delta": 0.0
    }
  }
}
